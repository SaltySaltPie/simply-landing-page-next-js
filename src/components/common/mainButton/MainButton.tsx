import styles from './MainButton.module.scss'
export const MainButton = ({}:TMainButtonProps) => {
  return (
    <div className={`${styles.contentC}`}>
       $1
    </div>
  );
};

type TMainButtonProps = {
}