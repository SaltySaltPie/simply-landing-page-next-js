import styles from "./WeAboutInvestors.module.scss";
export const WeAboutInvestors = () => {
   return (
      <div className={`${styles.contentC}`}>
         <h1>We are about</h1>
      </div>
   );
};
