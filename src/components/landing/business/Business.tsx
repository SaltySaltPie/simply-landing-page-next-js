import styles from './Business.module.scss'
export const Business = ({}:TBusinessProps) => {
  return (
    <div className={`${styles.contentC}`}>
       $1
    </div>
  );
};

type TBusinessProps = {
}