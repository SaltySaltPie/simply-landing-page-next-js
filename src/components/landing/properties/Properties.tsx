import styles from './Properties.module.scss'
export const Properties = ({}:TPropertiesProps) => {
  return (
    <div className={`${styles.contentC}`}>
       $1
    </div>
  );
};

type TPropertiesProps = {
}